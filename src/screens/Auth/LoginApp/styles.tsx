import {Spacing} from '@theme';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  logo: {
    width: Spacing.width288,
    height: Spacing.height243,
    marginTop: Spacing.height40,
    alignSelf: 'center',
    // backgroundColor: 'red',
  },
});
